import QtQuick
import QtQuick.Window

Window {
    visible: true
    width: 360
    height: 640
    title: "DubInstante"
    color: "#1a1a2e"

    Text {
        anchors.centerIn: parent
        text: "Hello World V8 - DubInstante TEST!"
        color: "white"
        font.pixelSize: 24
        font.bold: true
    }
}
